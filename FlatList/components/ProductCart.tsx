import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const ProductCart = ({item:Product}) => {
  return (
    <View>
      <Text>ProductCart</Text>
    </View>
  )
}

export default ProductCart

const styles = StyleSheet.create({})